version: '3.8'

services:
  back:
    build: ./back
    ports:
      - "8000:8000"
    container_name: fastapi_back

  front:
    build: ./front
    ports:
      - "8501:8501"
    container_name: streamlit_front
    depends_on:
      - back